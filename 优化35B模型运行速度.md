下面这版我不再简单拼接两份方案，而是把你上传的 GLM 方案与你前面让我做的 Vulkan/MoE 分析逐项交叉验证，并结合截至 **2026-08-18** 的 Qwen3.6 官方模型信息、当前 llama.cpp 的 MTP/Vulkan 实现以及 AMD RX 6700S 的官方规格重新收敛。

先给结论：

> **最终路线应以“单 dGPU 主计算 + CPU/RAM Expert 池 + Expert 热度/预测预取 + 专用 `MUL_MAT_ID` + MTP”作为主线。**
>
> **iGPU 多设备、SAM “零拷贝”、NUMA 感知分配、cooperative matrix 并不是第一阶段核心，甚至其中几项不能按照 GLM 原方案直接实现。它们应该变成经过实测后才启用的实验分支。**

你的目标设备是 **ROG Zephyrus G14 2022 GA402RJ，Ryzen 7 6800HS + RX 6700S 8GB + 40GB RAM**，而上传方案写成了 **Ryzen 9 6900HS + RX 6800S**。公开资料也能确认 GA402RJ 正是 R7 6800HS + RX 6700S，AMD 官方则给出了 RX 6700S 的 28 CU、8GB GDDR6、224 GB/s 和最高 80W 规格。([AMD][1])

---

# 一、最终版目标

### 目标硬件

| 项目     | 你的实际设备                         |
| ------ | ------------------------------ |
| 整机     | ASUS ROG Zephyrus G14 2022     |
| CPU    | AMD Ryzen 7 6800HS             |
| CPU    | Zen 3+，8C / 16T                |
| dGPU   | AMD Radeon RX 6700S            |
| GPU 架构 | RDNA2 / Navi23                 |
| CU     | 28                             |
| SP     | 1792                           |
| 显存     | 8GB GDDR6                      |
| 显存带宽   | 224 GB/s                       |
| GPU 功耗 | Up to 80W                      |
| iGPU   | Radeon 680M                    |
| 内存     | 40GB                           |
| OS     | Windows 11                     |
| 推理后端   | llama.cpp + Vulkan             |
| 二开     | ggml / ggml-vulkan / llama.cpp |

AMD 官方当前页面也确认 RX 6700S 支持 Windows 11、Smart Access Memory、SmartShift，并明确给出 28 CU、8GB、224GB/s 等规格。([AMD][1])

---

# 二、先纠正 GLM 方案中的几个关键问题

这是整合时最重要的一步。

## 1. GLM 的 CPU/GPU 型号写错

上传文件写的是：

> Ryzen 9 6900HS + RX 6800S。

但你的设备是：

> Ryzen 7 6800HS + RX 6700S 8GB。

因此 GLM 方案里所有：

* 10 TF FP16
* 32 CU
* 256 GB/s
* RX 6800S

相关性能结论都必须下调或者重新 benchmark。

你的 RX 6700S 官方峰值 FP32 为 7.92 TFLOPS、FP16 vector 为 15.84 TFLOPS、224 GB/s 显存带宽。([AMD][1])

所以我不会直接采用 GLM 给出的绝对 tok/s。

---

# 三、Qwen3.6-35B-A3B 的真实结构

这一部分两份方案基本一致，而且官方资料可以直接佐证。

Qwen 官方模型卡目前给出的结构是：

* 总参数：35B
* 激活参数：3B/token
* hidden：2048
* 40 layers
* 256 experts
* 8 Routed + 1 Shared
* Expert intermediate：512
* 10 个 Gated-Attention 层
* 30 个 Gated-DeltaNet 层
* GQA：16 Q / 2 KV
* MTP
* 原生上下文：262,144
* 可扩展到约 1.01M。([Hugging Face][2])

结构可以概括为：

```text
Embedding
   ↓
40 × Block

每个 Block：

┌───────────────────────────────┐
│  Gated DeltaNet / Attention   │
│              ↓                │
│              MoE              │
│     256 experts               │
│     8 routed + 1 shared       │
└───────────────────────────────┘

   ↓
LM Head
   ↓
MTP
```

更精确地说，是：

```text
10 × [
    DeltaNet
    → MoE
    → DeltaNet
    → MoE
    → DeltaNet
    → MoE
    → Gated Attention
    → MoE
]
```

也就是：

```text
30 × Gated DeltaNet
10 × Gated Attention
40 × MoE
```

这是整个优化方案的核心。

---

# 四、为什么这个模型特别适合你的机器

传统 Dense 35B：

```text
每个 token
↓
大量参数全部参与
```

Qwen3.6：

```text
35B 总参数
↓
每 token 约 3B active
↓
256 experts 中只选 8
+ shared expert
```

官方模型卡确认这是：

> 35B total / 3B activated / 8 routed + 1 shared。([Hugging Face][2])

所以：

```text
计算复杂度 ≈ 3B
存储复杂度 ≈ 35B
```

这就形成一个极其特殊的问题：

> **你不是算不动，而是需要高效地把“这次真正需要的 Expert”送到计算单元。**

所以你的机器最适合的不是：

```text
Layer Offload
```

而是：

```text
Expert Offload
```

---

# 五、最终的优化总架构

最终我建议把整个推理系统设计成：

```text
                     Qwen3.6 Runtime
                           │
                     Token Scheduler
                           │
                     Router / MTP
                           │
              ┌────────────┴─────────────┐
              │                          │
         Expert Scheduler           MTP Predictor
              │                          │
      ┌───────┼────────┐                 │
      │       │        │                 │
     HOT    WARM     COLD                │
      │       │        │                 │
    VRAM     RAM      mmap                │
      │       │        │                 │
      └───────┴────────┴────────┐         │
                                │         │
                          async prefetch  │
                                │         │
                                ▼         │
                       Grouped MUL_MAT_ID │
                                │         │
                                ▼         │
                        Fused Expert FFN  │
                                │         │
                                ▼         │
                         DeltaNet/Attn    │
                                │         │
                                └─────────┘
```

这就是最终版本的核心。

---

# 六、显存策略：不要再使用“传统 -ngl 思维”

GLM 方案有一个很好的方向：

> MoE 应该做 Expert tiering，而不是单纯层卸载。

这个方向我完全保留。

但我不会直接固定：

```text
Hot = 13
Warm = 38
Cold = 205
```

因为这个数字目前没有足够证据支持成为你的 RX 6700S 的固定最佳值。

上传方案把 Expert 访问分布直接假设成：

> top 5% 覆盖 60%+ activation。

这个假设要改成：

> **运行时测量，不预设。**

最近针对 Qwen3.6 的路由行为测试显示，连续 token 的 expert 集合存在约 35% 的重叠，两个相邻 token 的平均 unique experts 大约 13.2 个，这非常支持“预测/预取”，但不支持直接假定某 13 个 Expert 永久覆盖 60%。([GitHub][3])

因此最终设计：

```text
HOT/WARM/COLD = dynamic
```

而不是：

```text
13 / 38 / 205 hard-coded
```

---

# 七、GPU VRAM 最终预算

RX 6700S：

```text
8GB
```

建议第一版：

| 项目                     |         预算 |
| ---------------------- | ---------: |
| Vulkan/runtime         | 0.4–0.7 GB |
| 常驻 Backbone            | 1.5–2.5 GB |
| Shared Expert          | 0.3–0.6 GB |
| Attention / DeltaNet   | 1.0–1.5 GB |
| MTP                    |    <0.5 GB |
| Expert cache           | 2.0–3.0 GB |
| KV / scratch / staging | 0.7–1.2 GB |

不要追求：

```text
VRAM utilization = 99%
```

目标应该是：

```text
稳定 7~7.5GB
```

留出 Vulkan allocator 和临时 buffer 的空间。

---

# 八、RAM 才是第二层 Expert Cache

你的 40GB RAM 最适合：

```text
GPU VRAM
    ↓
RAM hot/warm
    ↓
mmap cold
```

建议：

```text
VRAM:
    active/hot expert

RAM:
    所有模型权重
    warm experts
    staging buffer

SSD:
    mmap backing store
```

不要在每次 Expert miss 时走：

```text
SSD
→ decompression
→ CPU
→ GPU
```

而应该：

```text
SSD
  ↓
mmap
  ↓
RAM resident
  ↓
async upload
  ↓
VRAM
```

---

# 九、我不同意 GLM 的“NUMA 感知内存分配”作为主方案

这是两套方案里最大的一个分歧。

GLM 建议：

```cpp
VirtualAllocExNuma(...)
```

然后假设：

```text
node 0 = 双通道 76.8GB/s
node 1 = 单通道 38.4GB/s
```

上传文件明确把 40GB 拆成：

> 前 16GB 双通道，后 24GB 单通道。

问题是：

> **双通道/单通道内存布局不是标准 NUMA Node 语义。**

`VirtualAllocExNuma()` 的 NUMA node 并不等价于：

```text
DDR5 channel A/B
```

所以不能直接通过：

```text
node 0 / node 1
```

获得你想要的“前 16GB 快、后 24GB 慢”。

### 最终方案改为：

```text
Memory Hotness Allocator
```

而不是：

```text
NUMA allocator
```

做：

```text
HOT pages
WARM pages
COLD pages
```

并结合：

* PrefetchVirtualMemory
* mmap
* page residency
* large allocation
* working-set monitoring

进行热页管理。

### 实际上应该测：

```text
buffer 1:
    latency/bandwidth

buffer 2:
    latency/bandwidth
```

确认 Windows 对这台机器的实际 memory behavior。

**先测，后决定。**

---

# 十、Expert Cache：这是整个项目第一核心

新增：

```text
ggml-vulkan/vk_expert_cache.*
```

核心：

```cpp
struct expert_key {
    uint16_t layer;
    uint16_t expert;
};

struct expert_entry {
    expert_key key;

    size_t host_offset;
    size_t size;

    VkBuffer gpu_buffer;
    VkDeviceSize gpu_offset;

    float ema_score;
    uint64_t last_used;
    uint32_t hit_count;
    uint32_t miss_count;

    enum class state {
        cold,
        warm,
        uploading,
        hot,
        evicting
    };
};
```

然后：

```text
ExpertCache
```

负责：

```text
lookup
admit
evict
prefetch
upload
statistics
```

---

# 十一、Expert Cache 不使用简单 LRU

最终推荐：

```text
Score =
    α × recent_frequency
  + β × EMA_frequency
  + γ × transition_probability
  + δ × layer_affinity
```

例如：

```text
score =
0.35 × recent
+
0.30 × EMA
+
0.25 × predicted
+
0.10 × layer
```

这样才是真正：

```text
MoE-aware cache
```

---

# 十二、Expert Cache 必须“按 layer 管理”

不要：

```text
expert #12
```

做全局唯一热度。

应该：

```text
(layer, expert)
```

例如：

```text
L03 E12
L11 E12
L27 E12
```

三个是三个独立 cache object。

因为：

> 同一个 Expert ID 在不同层并不代表相同计算。

---

# 十三、Shared Expert：永久驻留

GLM 没有把这一点突出，我建议加入最终架构。

每层：

```text
8 routed
+
1 shared
```

Shared Expert：

```text
always active
```

因此：

```text
Shared Expert
↓
Permanent GPU Residency
```

不要进入 LRU。

这可以消除：

```text
shared expert upload
```

和：

```text
descriptor update
```

。

---

# 十四、第二核心：Expert Prefetch

上传方案里的 MOD 7 很有价值，我保留。

但 predictor 顺序改成：

### 第一优先级

```text
Router probability
```

### 第二

```text
邻接 token expert transition
```

### 第三

```text
MTP prediction
```

### 第四

```text
ngram
```

原因是：

> 你不应该让 MTP 本身为了“预测 Expert”而额外承担模型执行成本。

---

# 十五、做 Expert Transition Matrix

对：

```text
256 experts
```

构建：

```text
256 × 256
```

转移概率：

```text
P(E_next | E_current)
```

运行几千/几万 token 后：

```text
E12
 ├── E51 31%
 ├── E87 18%
 ├── E19 12%
 ...
```

然后：

```text
当前:
E12

预取:
E51
E87
```

而不是把全部专家加载到 GPU。

---

# 十六、第三核心：`MUL_MAT_ID`

这是我和 GLM 方案共同确认的最大热点。

当前 llama.cpp Vulkan shader generator 已经明确存在：

```text
matmul_id
matmul_id_subgroup
mul_mat_vec_id
```

以及 integer-dot、subgroup、coopmat 等多个变体。([GitHub][4])

而当前社区已经有针对大 MoE 的实测：

> `MUL_MAT_ID` 可以成为大 MoE prefill 的主瓶颈。([GitHub][5])

另外一个针对 Qwen3.6-35B-A3B 的 AMD Vulkan 优化测试中：

```text
64×64
→
128×32
```

对 Q4_K_M prompt processing 提升约 **10.5%**，但这是 **Strix Halo + KHR_coopmat**，不是你的 RX6700S，因此只能证明“tile geometry 值得优化”，不能直接复制结果。([GitHub][6])

---

# 十七、所以 RX6700S 的 shader 策略要改

GLM 方案写：

```glsl
#extension GL_KHR_cooperative_matrix
```

并直接采用：

```text
16×16 coopmat
```

我不建议直接这么干。

### 原因

你的：

```text
RX6700S
RDNA2
gfx1032
```

和：

```text
Strix Halo
gfx1151
```

不是同一种 GPU。

所以：

> **cooperative matrix 必须先检测 extension + properties，再决定启用。**

默认：

```text
gfx1032:
    subgroup
    integer dot
    LDS
```

优先。

如果实际 Vulkan device reports：

```text
VK_KHR_cooperative_matrix
```

再进入实验路径。

---

# 十八、做 RX6700S 专用 Autotuner

建立：

```cpp
VkGpuProfile
```

识别：

```text
vendorID
deviceID
driverVersion
subgroupSize
sharedMemory
coopmatSupport
integerDotSupport
```

然后在启动时选择：

```text
32×32
64×32
64×64
128×32
128×64
```

不要硬编码一个 tile。

---

# 十九、Prefill 和 Decode 必须分 kernel

这是最终设计里必须保留的一点。

## Prefill

例如：

```text
tokens = 512
```

一个 Expert 可能同时处理：

```text
N = 100+
```

token。

适合：

```text
Grouped GEMM
```

---

## Decode

通常：

```text
N = 1
```

一个 token：

```text
8 routed experts
+
1 shared
```

这是 Tiny GEMM。

所以：

```text
decode kernel
```

必须单独优化。

---

# 二十、最终 `MUL_MAT_ID` 设计

最终做三条路径：

```text
MUL_MAT_ID
│
├── PREFILL
│     └── grouped expert GEMM
│
├── DECODE
│     └── tiny expert kernel
│
└── FALLBACK
      └── upstream kernel
```

调度：

```cpp
if (n_tokens >= 64)
    grouped_prefill();

else if (n_tokens > 1)
    grouped_decode_batch();

else
    tiny_decode();
```

---

# 二十一、不要简单地“9 个 expert 一个 dispatch”

GLM 提议：

```text
一次 dispatch 计算 9 experts
```

思路正确，但 shader 示例太理想化。

真正 GGUF：

```text
Q4_K
Q3_K
...
```

不能直接：

```glsl
float16_t weights[]
```

。

正确做法：

```text
Quantized Expert Weight
        ↓
Grouped Quant MatMul
        ↓
Accumulation
```

也就是说：

> **Grouped GEMM 应建立在现有 quantized Vulkan kernel 基础上，而不是重新发明一个 FP16 Expert kernel。**

---

# 二十二、Expert 权重布局必须针对 cache 优化

Qwen3.6 的 Expert：

* 256 experts
* intermediate 512
* Gate+Up 是融合结构
* Down 单独存在。([Hugging Face][7])

因此建议内部 packing：

```text
[layer]
 ├── expert[0]
 │    ├── gate_up
 │    └── down
 ├── expert[1]
 │    ├── gate_up
 │    └── down
 ...
```

进一步按 cache line / quant block 对齐。

核心目标：

```text
一次 Expert upload
```

尽量覆盖：

```text
gate_up + down
```

而不是拆成很多零散上传。

---

# 二十三、第四核心：Fused Expert FFN

标准过程：

```text
x
 │
 ├── gate
 │
 ├── up
 │
 ▼
SiLU(gate) × up
 │
 ▼
down
```

第一阶段不要强行把全部融合。

优先：

```text
gate_up
+
activation
```

减少：

```text
global write/read
```

第二阶段再考虑：

```text
gate_up + silu + down
```

完全融合。

---

# 二十四、第五核心：MTP

这一点 GLM 方案是正确的，而且现在已经不再只是实验性概念。

当前 llama.cpp master 的 speculative 文档已经明确支持：

```text
--spec-type draft-mtp
```

并且：

```text
--spec-draft-n-max
--spec-draft-p-min
```

等参数已经进入正式 speculative API。([GitHub][8])

Qwen3.6 官方也明确说明其训练包含 MTP。([Hugging Face][2])

---

# 二十五、但 MTP 必须放在后期验证

原因是当前 llama.cpp 近期确实出现过：

* MTP performance regression；
* acceptance 在某些 context boundary 下塌陷；
* deterministic output 差异等问题。([GitHub][9])

因此：

```text
MTP ≠ 开关一开必然 +100%
```

真正应该测：

```text
Baseline TG
MTP N=1
MTP N=2
MTP N=3
```

然后统计：

```text
acceptance rate
draft overhead
verify overhead
net tok/s
```

---

# 二十六、推荐 MTP 初始参数

第一版：

```text
n_max = 2
```

再测试：

```text
n_max = 3
```

最终选择：

```text
argmax(net throughput)
```

而不是：

```text
argmax(acceptance)
```

因为：

```text
acceptance = 80%
```

不意味着：

```text
net speed
```

最高。

---

# 二十七、不要做 GLM 的 Router Cache

GLM 提议：

```cpp
hidden_state_hash
→
routing_result
```

甚至用：

```text
threshold = 0.01
```

直接复用 Router 结果。

我不建议这样做。

因为：

> Router 输出不是可以安全近似缓存的纯函数结果。

一次错误的 Top-8：

```text
Expert selection
```

会改变整个后续计算。

### 最终改成：

```text
Router Cache
```

→

```text
Router Prediction Cache
```

只用于：

```text
prefetch
```

绝不用于：

```text
skip router
```

这是 correctness 与性能的关键边界。

---

# 二十八、iGPU：最终不是“默认开启”，而是实验路线

GLM 的核心假设：

> iGPU 680M 直接吃 RAM，因此拿来跑 DeltaNet / warm Expert。

理论方向有意义。

但这里有一个重大现实问题：

```text
dGPU = RX6700S
iGPU = 680M
```

它们是两个 Vulkan Physical Device。

不是：

```text
一个统一 GPU
```

你如果：

```text
CPU → iGPU
CPU → dGPU
```

分别建立：

```text
VkDevice
Queue
CommandPool
```

然后两个设备交替参与每层推理，就会产生：

```text
device synchronization
descriptor lifetime
command submission
cross-device ownership
```

等一系列成本。

所以最终：

# iGPU 不进 MVP。

---

# 二十九、最终 iGPU 三阶段策略

### Phase A

```text
iGPU OFF
```

建立稳定 baseline。

### Phase B

只让 iGPU 做：

```text
prefill-only
```

例如：

```text
DeltaNet prefill
```

与 dGPU：

```text
Attention
```

并行实验。

### Phase C

再测试：

```text
warm expert
```

是否值得上 iGPU。

如果：

```text
iGPU execution time
+
sync time
<
CPU/RAM path
```

才启用。

---

# 三十、SAM 也不要作为第一阶段“零拷贝方案”

GLM 提到：

> SAM = CPU 直接 mmap GDDR6。

这里需要纠正。

Resizable BAR/Smart Access Memory 的确扩大 CPU 对 GPU PCIe BAR 映射的可访问空间，但这并不意味着：

```text
普通 C/C++ pointer
=
Vulkan device-local VRAM pointer
```

更不能直接推出：

```text
OpenFileMapping()
+
MapViewOfFile()
=
CPU 直接安全 mmap Vulkan VRAM
```

上传方案中的：

```cpp
FILE_MAP_ALL_ACCESS
```

这一实现不能作为可靠的 Vulkan SAM zero-copy 方案。

---

# 三十一、SAM 最终只作为“后期实验”

第一阶段：

```text
Host → staging → VRAM
```

先把 pipeline 做到稳定。

第二阶段探索：

```text
HOST_VISIBLE / BAR memory
```

以及 AMD Vulkan driver 的实际 behavior。

只有实际 benchmark 证明：

```text
BAR read/write
>
staging
```

才启用。

---

# 三十二、最终 GPU/CPU 负责什么

这是最终版架构最关键的职责划分。

## RX6700S

负责：

```text
Attention
DeltaNet kernels
Shared Expert
Hot Expert
MTP
Grouped GEMM
sampling backend
```

## CPU

负责：

```text
Router
Expert scheduler
Expert cache
prefetch
mmap
cold/warm management
sampling fallback
HTTP/server
```

## RAM

负责：

```text
全部模型 backing store
warm experts
cold experts
staging
```

## SSD

只负责：

```text
mmap backing
cold start
```

---

# 三十三、最终不要做“CPU Expert vs GPU Expert”静态划分

而应该：

```text
Expert State Machine
```

：

```text
COLD
 ↓
WARM
 ↓
UPLOADING
 ↓
HOT
 ↓
EVICTING
 ↓
WARM
```

这样才是真正的 runtime。

---

# 三十四、推荐的新增模块

建议 fork 结构：

```text
ggml/src/ggml-vulkan/

    ggml-vulkan.cpp

    vk_gpu_profile.cpp
    vk_gpu_profile.hpp

    vk_expert_cache.cpp
    vk_expert_cache.hpp

    vk_expert_scheduler.cpp
    vk_expert_scheduler.hpp

    vk_expert_predictor.cpp
    vk_expert_predictor.hpp

    vk_expert_upload.cpp
    vk_expert_upload.hpp

    vk_memory_pool.cpp
    vk_memory_pool.hpp

    vk_autotune.cpp
    vk_autotune.hpp

    vk_timeline.cpp
    vk_timeline.hpp

    vulkan-shaders/
        matmul_id_expert.comp
        matmul_id_decode.comp
        matmul_id_prefill.comp
        expert_ffn_fused.comp
```

---

# 三十五、建议新增的内部接口

```cpp
class ExpertCache {
public:
    bool contains(const ExpertKey &key);

    ExpertEntry *lookup(const ExpertKey &key);

    void admit(const ExpertKey &key);

    void prefetch(const ExpertKey &key);

    void upload(const ExpertKey &key);

    void evict();

    ExpertStats stats() const;
};
```

Scheduler：

```cpp
class ExpertScheduler {
public:
    RoutingPlan plan(
        int layer,
        std::span<const ExpertId> experts);

    void schedule_prefetch(
        const RoutingPlan &plan);
};
```

Predictor：

```cpp
class ExpertPredictor {
public:
    std::vector<ExpertId>
    predict_next(
        const RoutingHistory &history,
        const MtpHint *mtp);
};
```

---

# 三十六、Vulkan backend 新增层

```text
CPU
 │
 ▼
ggml graph
 │
 ▼
Expert Scheduler
 │
 ├── cache hit
 └── cache miss
       │
       ▼
Transfer Queue
       │
       ▼
GPU VRAM
       │
       ▼
MUL_MAT_ID
```

同时：

```text
Compute Queue
```

负责：

```text
Current Expert Compute
```

通过 timeline semaphore：

```text
Upload[i]
   ↓
signal
   ↓
Compute waits
```

实现：

```text
DMA 与计算重叠
```

。

---

# 三十七、必须做 Transfer/Compute 双队列

这个优化我认为优先级很高：

```text
Transfer Queue
Compute Queue
```

形成：

```text
Compute:
██████████████████████

DMA:
   ███████
         ███████
               ███████
```

如果 Expert 上传等待计算全部结束：

```text
GPU idle
```

会非常严重。

---

# 三十八、Descriptor 不允许频繁创建

不要：

```text
expert miss
→ create descriptor
→ update
→ dispatch
```

而是：

```text
Large SSBO
+
expert_offset[layer][expert]
```

例如：

```cpp
struct ExpertLocation {
    uint32_t buffer_id;
    uint32_t offset;
    uint32_t size;
};
```

shader：

```text
base + expert_offset
```

这样 Expert residency 变化：

```text
offset table
```

变化即可。

---

# 三十九、MTP 和 Expert Prefetch 要联动

最终：

```text
Token N
 ↓
router
 ↓
current experts
 ↓
compute
 ↓
MTP
 ↓
predicted tokens
 ↓
predicted experts
 ↓
prefetch
 ↓
Token N+1
```

这比：

```text
MTP 单独优化
```

更有意义。

因为你的 GPU 真正的问题是：

```text
Expert data arrival
```

。

---

# 四十、上下文不要一开始冲 128K/256K

官方 Qwen3.6 的确原生支持 262,144 tokens。([Hugging Face][10])

但对于你的机器：

```text
8GB VRAM
40GB RAM
```

第一阶段建议：

```text
4K
8K
16K
32K
```

作为性能优化 workload。

等核心 runtime 稳定以后：

```text
64K
128K
256K
```

再测。

---

# 四十一、KV Cache：Q8 作为基线

Qwen3.6 只有：

```text
10 full attention layers
```

而不是 40 层。

所以相比传统 40 层 Attention 模型，KV 压力明显低。

第一阶段：

```text
K = Q8
V = Q8
```

第二阶段：

```text
Q4_0
```

才进入测试。

原因：

> 不应该为了省 200~500MB KV，而牺牲模型质量，同时让 Expert cache 失去更大的收益空间。

---

# 四十二、量化选择：最终我推荐“两档”

GLM 强推：

```text
UD-Q3_K_M
```

这是合理的性能方案；文件中的 Q3/K/Q4 对比和对应速度预测也给出了这一方向。

但你的最终版本不能只做 Q3。

### 质量版

```text
UD-Q4_K_M
```

当前 Hugging Face 官方 Unsloth 仓库直接提供这个量化。([Hugging Face][10])

### 性能版

```text
UD-Q3_K_M
```

### 极限版

```text
UD-IQ2_M
```

但：

```text
IQ2 ≠ 一定更快
```

尤其你的 Vulkan backend。

所以：

> **Q4_K_M 是开发基准，Q3_K_M 是性能基准，IQ2_M 是极限实验。**

---

# 四十三、为什么我不建议把 IQ2 当第一量化

因为你最终想优化：

```text
Expert MMQ
```

而低比特的实际速度取决于：

```text
shader support
integer dot
dequant overhead
memory pattern
```

而不是：

```text
file size
```

。

所以：

```text
smaller file
≠ faster Vulkan
```

必须实际 benchmark。

---

# 四十四、第一阶段 benchmark 必须建立完整矩阵

基准至少包括：

### 模型

```text
UD-Q4_K_M
UD-Q3_K_M
```

### Context

```text
4K
8K
16K
32K
```

### Prefill

```text
PP16
PP64
PP256
PP512
PP1024
```

### Decode

```text
TG1
TG32
```

### MTP

```text
OFF
N=1
N=2
N=3
```

---

# 四十五、还要测 Expert 指标

必须输出：

```text
expert_cache_hit_rate
expert_cache_miss_rate

expert_gpu_resident

expert_upload_bytes

expert_upload_latency

expert_prefetch_hit

expert_prediction_accuracy

MUL_MAT_ID_time

MUL_MAT_ID_dispatch_count
```

---

# 四十六、GPU 必须用 Vulkan Timestamp

不要：

```cpp
std::chrono
```

测整个函数。

而是：

```text
GPU Timestamp
```

分别测：

```text
router
expert upload
MUL_MAT_ID
DeltaNet
Attention
Shared Expert
MTP
sampling
```

这样最终能得到：

```text
Router              0.10 ms
Upload              0.45 ms
MUL_MAT_ID          1.80 ms
DeltaNet            0.55 ms
Attention           0.40 ms
MTP                 0.20 ms
```

然后才能决定下一步到底优化什么。

---

# 四十七、最终实施任务列表

## Phase 0：基线

**任务**

* 固定 llama.cpp commit
* 固定 AMD driver
* 固定 Vulkan SDK
* Q4_K_M
* context 8K
* `np=1`
* MTP OFF
* 记录 GPU/CPU/RAM 使用率

**目标**

得到：

```text
Stock baseline
```

---

## Phase 1：Instrumentation

### MOD-A

新增：

```text
Vulkan timestamp
GPU utilization
VRAM usage
Expert routing statistics
```

### 结果

必须回答：

```text
到底哪里慢？
```

---

## Phase 2：Expert Cache

### MOD-B

实现：

```text
ExpertCache
```

### 功能

```text
hot
warm
cold
LRU/LFU/EMA
```

---

## Phase 3：Async Upload

### MOD-C

实现：

```text
Transfer Queue
Compute Queue
Timeline Semaphore
Ring Buffer
```

---

## Phase 4：Expert Predictor

### MOD-D

实现：

```text
Expert transition matrix
```

然后：

```text
predict → prefetch
```

---

## Phase 5：`MUL_MAT_ID`

### MOD-E

实现：

```text
prefill grouped kernel
decode tiny kernel
```

---

## Phase 6：RX6700S Autotune

### MOD-F

测试：

```text
32×32
64×32
64×64
128×32
128×64
```

并自动选。

---

## Phase 7：Expert FFN Fusion

### MOD-G

第一阶段：

```text
gate_up + SiLU
```

第二阶段：

```text
full FFN fusion
```

---

## Phase 8：MTP

### MOD-H

启用：

```text
draft-mtp
```

测试：

```text
N=1/2/3
```

---

## Phase 9：iGPU

### MOD-I

实验：

```text
prefill-only
```

然后：

```text
DeltaNet
```

最后才：

```text
warm expert
```

---

## Phase 10：Advanced Memory

### MOD-J

实验：

```text
BAR
SAM
host-visible VRAM
```

---

# 四十八、最终优先级

我最终不会采用 GLM 的“10 个 MOD 平均推进”，而采用：

| 优先级 | 项目                      |   重要性 |
| --- | ----------------------- | ----: |
| S+  | Expert Cache            | ★★★★★ |
| S+  | Expert Prefetch         | ★★★★★ |
| S+  | `MUL_MAT_ID`            | ★★★★★ |
| S+  | MTP                     | ★★★★★ |
| S   | Async DMA               | ★★★★☆ |
| S   | RX6700S kernel autotune | ★★★★☆ |
| A   | Fused Expert FFN        | ★★★★☆ |
| A   | Shared Expert 常驻        | ★★★★☆ |
| A   | Expert transition model | ★★★★☆ |
| B   | iGPU Prefill            | ★★★☆☆ |
| C   | SAM/BAR                 | ★★☆☆☆ |
| C   | Router prediction cache | ★★☆☆☆ |
| D   | Router result cache     | ❌ 不建议 |

---

# 四十九、最终收益预期：重新校准

这里必须比 GLM 更谨慎。

GLM 给出的最终：

> 14–20 tok/s，极限 18–25 tok/s。

但这个数字不能直接作为你的 RX 6700S 目标，因为其硬件部分实际上基于 RX 6800S。

同时，当前 llama.cpp 社区公开的 Qwen3.6 benchmark 主要是在明显更强的 GPU 上，例如 RTX 4070 12GB，其 Q4_K_M TG128 可达约 76.6 t/s，这不能直接外推到你的 6700S。([GitHub][11])

因此我建议你的目标分成：

### 保守目标

```text
Stock Vulkan:
    4–7 tok/s

Optimized:
    8–12 tok/s
```

### 现实挑战目标

```text
10–15 tok/s
```

### 极佳结果

```text
15–18 tok/s
```

### 18+ tok/s

属于：

```text
Excellent / research-grade result
```

不能在开发前承诺。

---

# 五十、为什么我仍然认为 14~18 tok/s 是有机会的

因为你有几个组合优势：

```text
35B total
3B active
256 experts
8 routed
MTP
```

再叠加：

```text
RX6700S 224GB/s
8GB VRAM
40GB RAM
```

而社区已经证明：

```text
Qwen3.6-35B-A3B
+
Vulkan
+
MUL_MAT_ID specialization
```

存在明显优化空间。当前 AMD Vulkan 的 `MUL_MAT_ID` 优化已经出现 Qwen3.6 Q4_K_M 的约 10.5% prefill 改善，只是基准卡不是你的 RX6700S。([GitHub][6])

再加上：

```text
Expert cache
+
prefetch
+
MTP
+
DMA overlap
```

整体性能空间是存在的。

---

# 五十一、最终速度预期表

| 版本 | 模型     | MTP | Expert Cache | Kernel            |      预计 TG |
| -- | ------ | --: | -----------: | ----------------- | ---------: |
| V0 | Q4_K_M | OFF |          OFF | upstream          |        4–7 |
| V1 | Q4_K_M |  ON |          OFF | upstream          |        6–9 |
| V2 | Q4_K_M |  ON |        Basic | upstream          |       7–11 |
| V3 | Q4_K_M |  ON |      Dynamic | tuned MMID        |       9–13 |
| V4 | Q3_K_M |  ON |      Dynamic | tuned MMID        |      10–15 |
| V5 | Q3_K_M |  ON |   Predictive | tuned + fused     |      12–16 |
| V6 | Q3_K_M |  ON |   Predictive | full optimization |  **13–18** |
| V7 | IQ2_M  |  ON |   Predictive | experimental      | **15–20+** |

注意：

> 这些是**工程目标区间，不是测量结果**。

---

# 五十二、最值得追求的不是平均 tok/s

实际上对你这种单用户本地 AI 来说，更重要的是：

```text
TTFT
```

以及：

```text
P95 token latency
```

例如：

```text
平均：
14 tok/s

但偶尔：
150 ms/token
```

体验仍然很差。

因此最终 benchmark 应同时记录：

```text
TTFT
TG tok/s
P50 token latency
P95 token latency
P99 token latency
```

---

# 五十三、最终版架构图

```text
                       Windows 11
                           │
                           ▼
                 llama.cpp Runtime
                           │
                  ┌────────┴────────┐
                  │                 │
              CPU/RAM             Vulkan
                  │                 │
          Expert Scheduler         │
                  │                 │
        ┌─────────┼─────────┐      │
        │         │         │      │
       HOT       WARM      COLD    │
        │         │         │      │
      VRAM       RAM       mmap    │
        │         │         │      │
        └─────────┴────┬────┘      │
                       │           │
                 Async Transfer    │
                       │           │
                       ▼           ▼
                 RX6700S VRAM ──────────┐
                       │                │
             ┌─────────┴──────────┐     │
             │                    │     │
         Shared Expert        Routed Expert
             │                    │
             │              Grouped MMID
             │                    │
             └──────────┬─────────┘
                        │
                 DeltaNet / Attention
                        │
                        ▼
                      MTP
                        │
                        ▼
                  Expert Predictor
                        │
                        ▼
                    Prefetch
                        │
                        └──────────→ next token
```

---

# 五十四、最终二开原则

整个工程只遵守四条原则：

### 第一条

**不要按 Layer Offload 优化，要按 Expert Residency 优化。**

### 第二条

**不要把 GPU 当作“更快 CPU”，而要把它当作一个 8GB 的高速 Expert Cache + Compute Engine。**

### 第三条

**不要让 CPU→RAM→GPU 数据传输阻塞 GPU Compute。**

### 第四条

**任何优化必须用 Vulkan GPU Timestamp + Expert telemetry 证明，而不能依赖理论带宽计算。**

---

# 五十五、与 GLM 方案最终的取舍结论

| GLM 方案          | 最终判断                      |
| --------------- | ------------------------- |
| Expert Tiering  | ✅ 保留，升级为动态 cache          |
| Expert Prefetch | ✅ 保留，作为核心                 |
| Grouped GEMM    | ✅ 保留，重点加强                 |
| MTP             | ✅ 保留，最高优先级                |
| RDNA2 Shader    | ✅ 保留，但改成 gfx1032 autotune |
| NUMA Allocator  | ❌ 不直接采用                   |
| SAM zero-copy   | ⚠️ 实验项                    |
| iGPU 多设备        | ⚠️ 第二阶段实验                 |
| Peer Memory     | ⚠️ 高风险实验                  |
| Router Cache    | ❌ 不直接复用 routing result    |
| Q3_K_M          | ✅ 性能版                     |
| Q4_K_M          | ✅ 开发基准/质量版                |
| IQ2_M           | ⚠️ 极限实验                   |
| 14–20 tok/s     | ⚠️ 作为挑战目标，不作为承诺           |
| 18–25 tok/s     | ⚠️ 极限研究结果，不能预设            |

上传方案本身已经抓住了一个非常重要的方向：**“MoE 的核心不是算力分发，而是 expert 调度”**；这一点我完全认同。

而结合当前 llama.cpp 的实际 Vulkan 代码与社区实验后，我会把最终工程重心进一步收缩到：

> **Expert Cache → Prefetch → Async Transfer → `MUL_MAT_ID` → MTP**

这五个模块。

其中，当前 llama.cpp 已经正式支持 `draft-mtp`，而 Vulkan shader generator 已经有 `MUL_MAT_ID`、subgroup、integer-dot 等基础路径，因此你并不需要从零创造一个推理框架，而是需要在现有 ggml Vulkan backend 上建立一套 **Qwen3.6/35B-A3B 专属的 MoE runtime scheduler**。([GitHub][8])

**最终项目定位可以直接定成：**

```text
Qwen3.6-35B-A3B
        ×
RX 6700S 8GB
        ×
Windows 11
        ×
llama.cpp Vulkan
        ×
MoE Expert Runtime Optimization
```

这比单纯“魔改 llama.cpp”清晰得多，也更适合后面形成你自己的独立优化分支。

[1]: https://www.amd.com/en/support/downloads/drivers.html/graphics/radeon-rx/radeon-rx-6000-series/amd-radeon-rx-6700s.html?utm_source=chatgpt.com "AMD Radeon™ RX 6700S Drivers and Downloads | Latest Version"
[2]: https://huggingface.co/Qwen/Qwen3.6-35B-A3B?utm_source=chatgpt.com "Qwen/Qwen3.6-35B-A3B · Hugging Face"
[3]: https://github.com/ml-explore/mlx/discussions/3801?utm_source=chatgpt.com "Small-L fused expert gather for fine-grained MoE decode — measured expert-overlap data on GLM-5.2 (34% adjacent-token, depth-graded to 52%) · ml-explore mlx · Discussion #3801 · GitHub"
[4]: https://github.com/ggml-org/llama.cpp/blob/master/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp?utm_source=chatgpt.com "llama.cpp/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp at master · ggml-org/llama.cpp · GitHub"
[5]: https://github.com/ggml-org/llama.cpp/issues/21948?utm_source=chatgpt.com "Vulkan: MUL_MAT_ID is the dominant prefill bottleneck for large MoE models on gfx1151 · Issue #21948 · ggml-org/llama.cpp · GitHub"
[6]: https://github.com/ggml-org/llama.cpp/discussions/22598?utm_source=chatgpt.com "Tweaking tile geometry for MoE on AMD KHR_coopmat (Vulkan) · ggml-org llama.cpp · Discussion #22598 · GitHub"
[7]: https://huggingface.co/blog/EXDai/qwen36-35b-a3b-architecture-overview?utm_source=chatgpt.com "Qwen3.6-35B-A3B — Architecture Overview"
[8]: https://github.com/ggml-org/llama.cpp/blob/master/docs/speculative.md?utm_source=chatgpt.com "llama.cpp/docs/speculative.md at master · ggml-org/llama.cpp · GitHub"
[9]: https://github.com/ggml-org/llama.cpp/issues/25489?utm_source=chatgpt.com "MTP feature performance drops since b9935 · Issue #25489 · ggml-org/llama.cpp · GitHub"
[10]: https://huggingface.co/unsloth/Qwen3.6-35B-A3B-MTP-GGUF "unsloth/Qwen3.6-35B-A3B-MTP-GGUF · Hugging Face"
[11]: https://github.com/ggml-org/llama.cpp/discussions/21112?utm_source=chatgpt.com "Optimize my llama.cpp · ggml-org llama.cpp · Discussion #21112 · GitHub"
